var box= document.getElementById('circulo');

function randomColor(){

    var color = "";
    for (var i=0;i<6;i++){
        var rando = Math.floor(Math.random() * 10);
        rando = rando.toString();
        color += rando;
    }
    return color;
}

box.addEventListener('click',function(){
    var newColor = randomColor();
    box.style.backgroundColor = "#" + newColor;
});


var box1= document.getElementById('circulo1');

function randomColor(){

    var color = "";
    for (var i=0;i<6;i++){
        var rando = Math.floor(Math.random() * 10);
        rando = rando.toString();
        color += rando;
    }
    return color;
}

box1.addEventListener('click',function(){
    var newColor = randomColor();
    box1.style.backgroundColor = "#" + newColor;
});



var box2= document.getElementById('circulo2');

function randomColor(){

    var color = "";
    for (var i=0;i<6;i++){
        var rando = Math.floor(Math.random() * 10);
        rando = rando.toString();
        color += rando;
    }
    return color;
}

box2.addEventListener('click',function(){
    var newColor = randomColor();
    box2.style.backgroundColor = "#" + newColor;
});



var box3= document.getElementById('circulo3');

function randomColor(){

    var color = "";
    for (var i=0;i<6;i++){
        var rando = Math.floor(Math.random() * 10);
        rando = rando.toString();
        color += rando;
    }
    return color;
}

box3.addEventListener('click',function(){
    var newColor = randomColor();
    box3.style.backgroundColor = "#" + newColor;
});

var box4= document.getElementById('circulo4');

function randomColor(){

    var color = "";
    for (var i=0;i<6;i++){
        var rando = Math.floor(Math.random() * 10);
        rando = rando.toString();
        color += rando;
    }
    return color;
}

box4.addEventListener('click',function(){
    var newColor = randomColor();
    box4.style.backgroundColor = "#" + newColor;
});


var box5= document.getElementById('circulo5');

function randomColor(){

    var color = "";
    for (var i=0;i<6;i++){
        var rando = Math.floor(Math.random() * 10);
        rando = rando.toString();
        color += rando;
    }
    return color;
}

box5.addEventListener('click',function(){
    var newColor = randomColor();
    box5.style.backgroundColor = "#" + newColor;
});


var box6= document.getElementById('circulo6');

function randomColor(){

    var color = "";
    for (var i=0;i<6;i++){
        var rando = Math.floor(Math.random() * 10);
        rando = rando.toString();
        color += rando;
    }
    return color;
}

box6.addEventListener('click',function(){
    var newColor = randomColor();
    box6.style.backgroundColor = "#" + newColor;
});
